/** Returns the sum of a and b
 * @param {Number} a
 * @param {Number} b
 * @returns {Number} Sum of a and b
 * @test sum(1,1) == 2  // two
 * @test @pending sum(4,2) == 6
 * @test sum(7,-3) == 4
*/
function sum(a,b) {
  return a + b;
}

